services:
  - type: web
    name: kinito-serveur
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn serveur:app --host 0.0.0.0 --port $PORT"
